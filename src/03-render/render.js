import ReactDOM from 'react-dom'

const element = <h1>Hello, world!</h1>;
// TODO: draw element to #app

const time = (
  <div>
    <h1>current time: {new Date().toLocaleTimeString()}</h1>
  </div>
)

// TODO: draw time to #app

// TODO: update time every second(drawto #app)
// TODO: check update in browser tools
function tick() {

}

export default null
